from setuptools import setup

setup(
    name='RealEstateRoiCalculator',
    version='1.0',
    description='Module to calculate ROIs based on real estate data',
    author='followjack.dev',
    # packages=['foo'],  # same as name
    # install_requires=['bar', 'greek'],  # external packages as dependencies
)
